const App = () => {
  return (
    <main className="app-shell">
      <section className="card">
        <h1>AutoPilot DevOps</h1>
        <p>Frontend scaffold is ready. Backend API and Docker baseline are configured.</p>
      </section>
    </main>
  );
};

export default App;